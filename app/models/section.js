class Section {
    constructor(id, courseid, timescale, title, uri) {
        this.id = id,
        this.courseid = courseid,
        this.timescale = timescale,
        this.title = title
        this.uri = uri
    }
}

export default Section;